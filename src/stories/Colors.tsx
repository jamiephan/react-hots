export const W = (props: React.HTMLAttributes<HTMLSpanElement>) => (
  <span style={{ color: "#bed3fc" }}>{props.children}</span>
);

export const R = (props: React.HTMLAttributes<HTMLSpanElement>) => (
  <span style={{ color: "#ff8b8b" }}>{props.children}</span>
);

export const Y = (props: React.HTMLAttributes<HTMLSpanElement>) => (
  <span style={{ color: "#f3f285" }}>{props.children}</span>
);

export const G = (props: React.HTMLAttributes<HTMLSpanElement>) => (
  <span style={{ color: "#00fa8e" }}>{props.children}</span>
);
